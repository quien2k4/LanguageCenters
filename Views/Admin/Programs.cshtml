@model LanguageCenter.Models.AdminProgramPageViewModel

@{
    ViewBag.Title = "Quản lý chương trình";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container py-4">
    <div class="admin-page-header mb-4">
        <div>
            <h2 class="fw-bold mb-1">Quản lý chương trình</h2>
            <p class="text-muted mb-0">Thêm, cập nhật và ngừng hoạt động các chương trình ngoại ngữ.</p>
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">@TempData["Success"]</div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">@TempData["Error"]</div>
    }

    <div class="card shadow-sm border-0 mb-4">
        <div class="card-body">
            @using (Html.BeginForm("Programs", "Admin", FormMethod.Get, new { @class = "row g-3 align-items-end admin-filter-form" }))
            {
                <div class="col-lg-3 col-md-6">
                    <label for="search" class="form-label">Tìm kiếm</label>
                    <input type="text" id="search" name="search" value="@Model.Search" class="form-control" placeholder="Tên chương trình" />
                </div>
                <div class="col-lg-2 col-md-6">
                    <label for="level" class="form-label">Trình độ</label>
                    <select id="level" name="level" class="form-select">
                        <option value="">Tất cả trình độ</option>
                        @foreach (var item in Model.Levels)
                        {
                            <option value="@item" @(Model.Level == item ? "selected" : "")>@item</option>
                        }
                    </select>
                </div>
                <div class="col-lg-2 col-md-6">
                    <label for="status" class="form-label">Trạng thái</label>
                    <select id="status" name="status" class="form-select">
                        <option value="All" @(Model.Status == "All" ? "selected" : "")>Tất cả</option>
                        <option value="Active" @(Model.Status == "Active" ? "selected" : "")>Đang hoạt động</option>
                        <option value="Inactive" @(Model.Status == "Inactive" ? "selected" : "")>Ngừng hoạt động</option>
                    </select>
                </div>
                <div class="col-lg-5 col-md-12">
                    <div class="admin-filter-actions">
                        <button type="submit" class="btn btn-primary">Tìm kiếm</button>
                        <a href="@Url.Action("Programs", "Admin")" class="btn btn-outline-secondary">Đặt lại</a>
                        <a href="@Url.Action("CreateProgram", "Admin")" class="btn btn-success">Thêm mới</a>
                    </div>
                </div>
            }
        </div>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body p-0">
            @if (Model.Programs.Any())
            {
                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Mã chương trình</th>
                                <th>Hình ảnh</th>
                                <th>Tên chương trình</th>
                                <th>Trình độ</th>
                                <th>Thời lượng</th>
                                <th>Học phí</th>
                                <th>Hoạt động</th>
                                <th class="text-end">Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Programs)
                            {
                                var imageUrl = string.IsNullOrWhiteSpace(item.ImageURL)
                                    ? Url.Content("~/Content/Images/default-program.jpg")
                                    : Url.Content(item.ImageURL);

                                <tr>
                                    <td>@item.ProgramID</td>
                                    <td>
                                        <img src="@imageUrl" alt="@item.ProgramName" class="rounded" style="width: 84px; height: 56px; object-fit: cover;" />
                                    </td>
                                    <td class="fw-semibold">@item.ProgramName</td>
                                    <td>@(string.IsNullOrWhiteSpace(item.Level) ? "Đang cập nhật" : item.Level)</td>
                                    <td>@(string.IsNullOrWhiteSpace(item.Duration) ? "Đang cập nhật" : item.Duration)</td>
                                    <td>@item.Fee.ToString("N0")</td>
                                    <td>
                                        <span class="badge @(item.IsActive ? "bg-success" : "bg-secondary")">
                                            @(item.IsActive ? "Đang hoạt động" : "Ngừng hoạt động")
                                        </span>
                                    </td>
                                    <td class="text-end">
                                        <div class="d-flex justify-content-end gap-2">
                                            <a href="@Url.Action("EditProgram", "Admin", new { id = item.ProgramID })" class="btn btn-sm btn-outline-primary">Sửa</a>
                                            <a href="@Url.Action("DeleteProgram", "Admin", new { id = item.ProgramID })" class="btn btn-sm btn-outline-danger">Xóa</a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                if (Model.TotalPages > 1)
                {
                    <nav class="p-3" aria-label="Phân trang chương trình">
                        <ul class="pagination justify-content-center mb-0">
                            <li class="page-item @(Model.CurrentPage == 1 ? "disabled" : "")">
                                <a class="page-link" href="@Url.Action("Programs", "Admin", new { search = Model.Search, level = Model.Level, status = Model.Status, page = Model.CurrentPage - 1 })">Trước</a>
                            </li>
                            @for (var i = 1; i <= Model.TotalPages; i++)
                            {
                                <li class="page-item @(Model.CurrentPage == i ? "active" : "")">
                                    <a class="page-link" href="@Url.Action("Programs", "Admin", new { search = Model.Search, level = Model.Level, status = Model.Status, page = i })">@i</a>
                                </li>
                            }
                            <li class="page-item @(Model.CurrentPage == Model.TotalPages ? "disabled" : "")">
                                <a class="page-link" href="@Url.Action("Programs", "Admin", new { search = Model.Search, level = Model.Level, status = Model.Status, page = Model.CurrentPage + 1 })">Sau</a>
                            </li>
                        </ul>
                    </nav>
                }
            }
            else
            {
                <div class="p-4">
                    <div class="alert alert-info mb-0">Không tìm thấy chương trình.</div>
                </div>
            }
        </div>
    </div>
</div>
