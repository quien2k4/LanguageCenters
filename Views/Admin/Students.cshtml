@model LanguageCenter.Models.AdminStudentPageViewModel

@{
    ViewBag.Title = "Quản lý học viên";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container py-4">
    <div class="admin-page-header mb-4">
        <div>
            <h2 class="fw-bold mb-1">Quản lý học viên</h2>
            <p class="text-muted mb-0">Thêm, cập nhật và ngừng hoạt động tài khoản học viên.</p>
        </div>
    </div>

    <div class="card shadow-sm border-0 mb-4">
        <div class="card-body">
            @using (Html.BeginForm("Students", "Admin", FormMethod.Get, new { @class = "row g-3 align-items-end admin-filter-form" }))
            {
                <div class="col-lg-5 col-md-6">
                    <label for="search" class="form-label">Tìm kiếm</label>
                    <input type="text" id="search" name="search" value="@Model.Search" class="form-control" placeholder="Họ tên, email, số điện thoại" />
                </div>
                <div class="col-lg-3 col-md-6">
                    <label for="status" class="form-label">Trạng thái</label>
                    <select id="status" name="status" class="form-select">
                        <option value="All" @(Model.Status == "All" ? "selected" : "")>Tất cả</option>
                        <option value="Active" @(Model.Status == "Active" ? "selected" : "")>Đang hoạt động</option>
                        <option value="Inactive" @(Model.Status == "Inactive" ? "selected" : "")>Ngừng hoạt động</option>
                    </select>
                </div>
                <div class="col-lg-4 col-md-12">
                    <div class="admin-filter-actions">
                        <button type="submit" class="btn btn-primary">Tìm kiếm</button>
                        <a href="@Url.Action("Students", "Admin")" class="btn btn-outline-secondary">Đặt lại</a>
                        <a href="@Url.Action("CreateStudent", "Admin")" class="btn btn-success">Thêm mới</a>
                    </div>
                </div>
            }
        </div>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body p-0">
            @if (Model.Students.Any())
            {
                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Mã học viên</th>
                                <th>Ảnh đại diện</th>
                                <th>Họ tên</th>
                                <th>Email</th>
                                <th>Ngày sinh</th>
                                <th>Điện thoại</th>
                                <th>Hoạt động</th>
                                <th>Đăng ký</th>
                                <th>Thanh toán</th>
                                <th>Bài kiểm tra</th>
                                <th class="text-end">Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Students)
                            {
                                var avatar = string.IsNullOrWhiteSpace(item.Avatar)
                                    ? Url.Content("~/Content/Images/default-avatar.png")
                                    : Url.Content(item.Avatar);

                                <tr>
                                    <td>@item.StudentID</td>
                                    <td><img src="@avatar" alt="@item.FullName" class="rounded-circle" style="width: 48px; height: 48px; object-fit: cover;" /></td>
                                    <td class="fw-semibold">@item.FullName</td>
                                    <td>@item.Email</td>
                                    <td>@(item.DateOfBirth.HasValue ? item.DateOfBirth.Value.ToString("dd/MM/yyyy") : "Đang cập nhật")</td>
                                    <td>@(string.IsNullOrWhiteSpace(item.PhoneNumber) ? "Đang cập nhật" : item.PhoneNumber)</td>
                                    <td><span class="badge @(item.IsActive ? "bg-success" : "bg-secondary")">@(item.IsActive ? "Đang hoạt động" : "Ngừng hoạt động")</span></td>
                                    <td>@item.RegistrationCount</td>
                                    <td>@item.PaymentCount</td>
                                    <td>@item.PlacementTestCount</td>
                                    <td class="text-end">
                                        <div class="d-flex justify-content-end gap-2">
                                            <a href="@Url.Action("EditStudent", "Admin", new { id = item.StudentID })" class="btn btn-sm btn-outline-primary">Sửa</a>
                                            <a href="@Url.Action("DeleteStudent", "Admin", new { id = item.StudentID })" class="btn btn-sm btn-outline-danger">Ngừng hoạt động</a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                if (Model.TotalPages > 1)
                {
                    <nav class="p-3" aria-label="Phân trang học viên">
                        <ul class="pagination justify-content-center mb-0">
                            <li class="page-item @(Model.CurrentPage == 1 ? "disabled" : "")">
                                <a class="page-link" href="@Url.Action("Students", "Admin", new { search = Model.Search, status = Model.Status, page = Model.CurrentPage - 1 })">Trước</a>
                            </li>
                            @for (var i = 1; i <= Model.TotalPages; i++)
                            {
                                <li class="page-item @(Model.CurrentPage == i ? "active" : "")">
                                    <a class="page-link" href="@Url.Action("Students", "Admin", new { search = Model.Search, status = Model.Status, page = i })">@i</a>
                                </li>
                            }
                            <li class="page-item @(Model.CurrentPage == Model.TotalPages ? "disabled" : "")">
                                <a class="page-link" href="@Url.Action("Students", "Admin", new { search = Model.Search, status = Model.Status, page = Model.CurrentPage + 1 })">Sau</a>
                            </li>
                        </ul>
                    </nav>
                }
            }
            else
            {
                <div class="p-4">
                    <div class="alert alert-info mb-0">Không tìm thấy học viên.</div>
                </div>
            }
        </div>
    </div>
</div>
