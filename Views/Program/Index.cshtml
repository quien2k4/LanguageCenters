@model LanguageCenter.Models.ProgramListViewModel

@{
    ViewBag.Title = "Programs";
    Layout = "~/Views/Shared/_PublicLayout.cshtml";
}

<section class="bg-light py-5">
    <div class="container">
        <div class="row align-items-end g-3">
            <div class="col-lg-7">
                <h1 class="fw-bold mb-2">Programs</h1>
                <p class="text-muted mb-0">Find an active language program that matches your goal.</p>
            </div>
            <div class="col-lg-5">
                @using (Html.BeginForm("Index", "Program", FormMethod.Get, new { @class = "row g-2" }))
                {
                    <div class="col-md-5">
                        <input type="text" name="search" value="@Model.Search" class="form-control" placeholder="Search program" />
                    </div>
                    <div class="col-md-4">
                        <select name="level" class="form-select">
                            <option value="">All levels</option>
                            @foreach (var item in Model.Levels)
                            {
                                <option value="@item" @(Model.Level == item ? "selected" : "")>@item</option>
                            }
                        </select>
                    </div>
                    <div class="col-md-3">
                        <button type="submit" class="btn btn-primary w-100">Search</button>
                    </div>
                }
            </div>
        </div>
    </div>
</section>

<section class="py-5">
    <div class="container">
        @if (Model.Programs.Any())
        {
            <div class="row g-4">
                @foreach (var program in Model.Programs)
                {
                    var imageUrl = string.IsNullOrWhiteSpace(program.ImageURL)
                        ? Url.Content("~/Content/Images/default-program.jpg")
                        : Url.Content(program.ImageURL);

                    <div class="col-md-6 col-lg-4">
                        <div class="card h-100 shadow-sm border-0">
                            <img src="@imageUrl" class="card-img-top" alt="@program.ProgramName" style="height: 210px; object-fit: cover;" />
                            <div class="card-body d-flex flex-column">
                                <h5 class="card-title">@program.ProgramName</h5>
                                <p class="mb-1"><strong>Level:</strong> @(string.IsNullOrWhiteSpace(program.Level) ? "Updating" : program.Level)</p>
                                <p class="mb-1"><strong>Duration:</strong> @(string.IsNullOrWhiteSpace(program.Duration) ? "Updating" : program.Duration)</p>
                                <p class="mb-3"><strong>Fee:</strong> @program.Fee.ToString("N0")</p>
                                <a href="@Url.Action("Detail", "Program", new { id = program.ProgramID })" class="btn btn-outline-primary mt-auto">View Detail</a>
                            </div>
                        </div>
                    </div>
                }
            </div>

            if (Model.TotalPages > 1)
            {
                <nav class="mt-5" aria-label="Program pagination">
                    <ul class="pagination justify-content-center">
                        <li class="page-item @(Model.CurrentPage == 1 ? "disabled" : "")">
                            <a class="page-link" href="@Url.Action("Index", "Program", new { search = Model.Search, level = Model.Level, page = Model.CurrentPage - 1 })">Previous</a>
                        </li>

                        @for (var i = 1; i <= Model.TotalPages; i++)
                        {
                            <li class="page-item @(Model.CurrentPage == i ? "active" : "")">
                                <a class="page-link" href="@Url.Action("Index", "Program", new { search = Model.Search, level = Model.Level, page = i })">@i</a>
                            </li>
                        }

                        <li class="page-item @(Model.CurrentPage == Model.TotalPages ? "disabled" : "")">
                            <a class="page-link" href="@Url.Action("Index", "Program", new { search = Model.Search, level = Model.Level, page = Model.CurrentPage + 1 })">Next</a>
                        </li>
                    </ul>
                </nav>
            }
        }
        else
        {
            <div class="alert alert-info mb-0">No programs found.</div>
        }
    </div>
</section>
