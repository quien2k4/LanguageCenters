@{
    var avatar = Session["Avatar"] == null || string.IsNullOrEmpty(Session["Avatar"].ToString())
        ? Url.Content("~/Content/Images/default-avatar.png")
        : Url.Content(Session["Avatar"].ToString());
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title - Quản trị</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <link href="~/Content/dashboard.css" rel="stylesheet" />
</head>
<body>

    <div class="dashboard-wrapper">

        <aside class="sidebar admin-sidebar">
            <div class="sidebar-logo">
                Bảng quản trị
            </div>

            <ul class="sidebar-menu">
                <li>
                    <a href="@Url.Action("Index", "Home")" class="sidebar-home-link">Về trang chủ</a>
                </li>
                <li>
                    <a href="@Url.Action("Dashboard", "Admin")">Bảng điều khiển</a>
                </li>              
                <li>
                    <a href="@Url.Action("Programs", "Admin")">Quản lý chương trình</a>
                </li>
                <li>
                    <a href="@Url.Action("Classes", "Admin")">Quản lý lớp học</a>
                </li>
                <li>
                    <a href="@Url.Action("Teachers", "Admin")">Quản lý giáo viên</a>
                </li>
                <li>
                    <a href="@Url.Action("Students", "Admin")">Quản lý học viên</a>
                </li>
                <li>
                    <a href="@Url.Action("Registrations", "Admin")">Quản lý đăng ký</a>
                </li>
                <li>
                    <a href="@Url.Action("Payments", "Admin")">Quản lý thanh toán</a>
                </li>
                <li>
                    <a href="@Url.Action("PlacementTests", "Admin")">Quản lý kiểm tra đầu vào</a>
                </li>
                <li>
                    <a href="@Url.Action("Consultations", "Admin")">Quản lý tư vấn</a>
                </li>
                <li>
                    <a href="@Url.Action("Logout", "Account")">Đăng xuất</a>
                </li>
            </ul>
        </aside>

        <div class="main-area">

            <header class="topbar">
                <h4>@ViewBag.Title</h4>

                <div class="user-box">
                    <img src="@avatar" class="avatar-small" />
                    <span>@Session["FullName"]</span>
                </div>
            </header>

            <section class="content">
                @if (TempData["Success"] != null)
                {
                    <div class="alert alert-success">
                        @TempData["Success"]
                    </div>
                }

                @if (TempData["Error"] != null)
                {
                    <div class="alert alert-danger">
                        @TempData["Error"]
                    </div>
                }

                @RenderBody()
            </section>

        </div>
    </div>

    <script src="~/Scripts/jquery-3.7.1.min.js"></script>
    <script src="~/Scripts/bootstrap.bundle.min.js"></script>

    @RenderSection("scripts", required: false)
</body>
</html>
