@{
    var avatar = Session["Avatar"] == null || string.IsNullOrEmpty(Session["Avatar"].ToString())
        ? Url.Content("~/Content/Images/default-avatar.png")
        : Url.Content(Session["Avatar"].ToString());
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title - Học viên</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <link href="~/Content/dashboard.css" rel="stylesheet" />
</head>
<body>

    <div class="dashboard-wrapper">

        <aside class="sidebar">
            <div class="sidebar-logo">
                Trung tâm Ngoại ngữ
            </div>

            <ul class="sidebar-menu">
                <li>
                    <a href="@Url.Action("Index", "Program")" class="sidebar-home-link">Quay lại chương trình</a>
                </li>
                <li>
                    <a href="@Url.Action("Profile", "Student")">Hồ sơ của tôi</a>
                </li>
                
                <li>
                    <a href="@Url.Action("MyClasses", "Student")">Lớp học của tôi</a>
                </li>
                <li>
                    <a href="@Url.Action("Payments", "Student")">Thanh toán</a>
                </li>
                <li>
                    <a href="@Url.Action("PlacementTests", "Student")">Kiểm tra đầu vào</a>
                </li>
                <li>
                    <a href="@Url.Action("Consultation", "Student")">Tư vấn</a>
                </li>
                <li>
                    <a href="@Url.Action("Logout", "Account")">Đăng xuất</a>
                </li>
            </ul>
        </aside>

        <div class="main-area">

            <header class="topbar">
                <h4>@ViewBag.Title</h4>

                <div class="user-box">
                    <img src="@avatar" class="avatar-small" />
                    <span>@Session["FullName"]</span>
                </div>
            </header>

            <section class="content">
                @if (TempData["Success"] != null)
                {
                    <div class="alert alert-success">
                        @TempData["Success"]
                    </div>
                }

                @if (TempData["Error"] != null)
                {
                    <div class="alert alert-danger">
                        @TempData["Error"]
                    </div>
                }

                @RenderBody()
            </section>

        </div>
    </div>

    <script src="~/Scripts/jquery-3.7.1.min.js"></script>
    <script src="~/Scripts/bootstrap.bundle.min.js"></script>

    @RenderSection("scripts", required: false)
</body>
</html>
