@model LanguageCenter.Models.CreateConsultationViewModel

@{
    ViewBag.Title = "Send Consultation Request";
    Layout = "~/Views/Shared/_StudentLayout.cshtml";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">Send Consultation Request</h4>
                </div>
                <div class="card-body p-4">
                    @if (TempData["Error"] != null)
                    {
                        <div class="alert alert-danger">@TempData["Error"]</div>
                    }

                    @using (Html.BeginForm("CreateConsultation", "Student", FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()

                        <div class="mb-3">
                            @Html.LabelFor(model => model.ContactInformation, new { @class = "form-label" })
                            @Html.TextBoxFor(model => model.ContactInformation, new { @class = "form-control", required = "required" })
                            @Html.ValidationMessageFor(model => model.ContactInformation, "", new { @class = "text-danger" })
                        </div>

                        <div class="mb-4">
                            @Html.LabelFor(model => model.QuestionContent, new { @class = "form-label" })
                            @Html.TextAreaFor(model => model.QuestionContent, 6, 40, new { @class = "form-control", required = "required" })
                            @Html.ValidationMessageFor(model => model.QuestionContent, "", new { @class = "text-danger" })
                        </div>

                        <div class="d-flex flex-wrap gap-2">
                            <button type="submit" class="btn btn-primary">Send Request</button>
                            <a href="@Url.Action("Consultation", "Student")" class="btn btn-outline-secondary">Back to Consultation</a>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
