@model LanguageCenter.Models.TeacherMaterialsViewModel

@{
    ViewBag.Title = "Tài liệu lớp học";
    Layout = "~/Views/Shared/_TeacherLayout.cshtml";
}

<div class="container py-4">
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-4">
        <div>
            <h2 class="fw-bold mb-1">Tài liệu lớp học</h2>
            <p class="text-muted mb-0">Tải lên và quản lý tài liệu học tập cho lớp này.</p>
        </div>
        <a href="@Url.Action("MyClasses", "Teacher")" class="btn btn-outline-primary">Quay lại lớp của tôi</a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">@TempData["Success"]</div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">@TempData["Error"]</div>
    }

    <div class="card shadow-sm border-0 mb-4">
        <div class="card-header bg-primary text-white">
            <h5 class="mb-0">@Model.ClassName</h5>
        </div>
        <div class="card-body">
            <div class="row g-3">
                <div class="col-md-4">
                    <div class="border rounded p-3 h-100">
                        <div class="text-muted small">Mã lớp</div>
                        <div class="fw-semibold">@Model.ClassID</div>
                    </div>
                </div>
                <div class="col-md-8">
                    <div class="border rounded p-3 h-100">
                        <div class="text-muted small">Chương trình</div>
                        <div class="fw-semibold">@(string.IsNullOrWhiteSpace(Model.ProgramName) ? "Đang cập nhật" : Model.ProgramName)</div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="card shadow-sm border-0 mb-4">
        <div class="card-header bg-white">
            <h5 class="mb-0">Tải tài liệu lên</h5>
        </div>
        <div class="card-body">
            @using (Html.BeginForm("UploadMaterial", "Teacher", new { classId = Model.ClassID }, FormMethod.Post, new { enctype = "multipart/form-data" }))
            {
                @Html.AntiForgeryToken()

                <div class="row g-3 align-items-end">
                    <div class="col-md-4">
                        <label for="title" class="form-label">Tiêu đề</label>
                        <input type="text" id="title" name="title" class="form-control" required="required" />
                    </div>
                    <div class="col-md-5">
                        <label for="file" class="form-label">Tệp</label>
                        <input type="file" id="file" name="file" class="form-control" required="required" accept=".pdf,.doc,.docx,.ppt,.pptx,.txt" />
                    </div>
                    <div class="col-md-3">
                        <button type="submit" class="btn btn-primary w-100">Tải lên</button>
                    </div>
                </div>
                <div class="form-text mt-2">Định dạng cho phép: pdf, doc, docx, ppt, pptx, txt.</div>
            }
        </div>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-header bg-white">
            <h5 class="mb-0">Tài liệu đã tải lên</h5>
        </div>
        <div class="card-body p-0">
            @if (Model.Materials.Any())
            {
                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Mã tài liệu</th>
                                <th>Tiêu đề</th>
                                <th>Đường dẫn tệp</th>
                                <th>Ngày tải lên</th>
                                <th class="text-end">Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Materials)
                            {
                                <tr>
                                    <td>@item.MaterialID</td>
                                    <td class="fw-semibold">@item.Title</td>
                                    <td>@item.FilePath</td>
                                    <td>@(item.UploadDate.HasValue ? item.UploadDate.Value.ToString("dd/MM/yyyy HH:mm") : "Đang cập nhật")</td>
                                    <td class="text-end">
                                        <div class="d-flex justify-content-end gap-2">
                                            <a href="@Url.Action("DownloadMaterial", "Teacher", new { materialId = item.MaterialID })" class="btn btn-sm btn-outline-primary">Tải xuống</a>

                                            @using (Html.BeginForm("DeleteMaterial", "Teacher", new { materialId = item.MaterialID }, FormMethod.Post))
                                            {
                                                @Html.AntiForgeryToken()
                                                <button type="submit" class="btn btn-sm btn-outline-danger">Xóa</button>
                                            }
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="p-4">
                    <div class="alert alert-info mb-0">Chưa có tài liệu nào được tải lên.</div>
                </div>
            }
        </div>
    </div>
</div>
